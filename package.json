{
  "name": "memecoin-analysis-engine",
  "private": true,
  "version": "0.1.0",
  "description": "Algorithm-first, explainable due-diligence engine for memecoins.",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* run build && pnpm --filter @mae/web run build",
    "test": "pnpm -r --filter=./packages/* run test",
    "typecheck": "pnpm -r run typecheck",
    "dev:web": "pnpm --filter @mae/web run dev",
    "dev:worker": "pnpm --filter @mae/worker run dev"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.0",
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
